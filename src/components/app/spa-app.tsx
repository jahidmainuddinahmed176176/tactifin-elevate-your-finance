import React from "react";

export function SpaApp() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold">Tactifin (lite)</h1>
      <p className="mt-2 text-sm text-muted-foreground">A lightweight build to recover from the recent edit. Restore full app from Git history when ready.</p>
    </div>
  );
}
